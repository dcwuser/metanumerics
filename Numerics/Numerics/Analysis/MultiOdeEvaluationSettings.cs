using System;
using System.Collections.Generic;


namespace Meta.Numerics.Analysis {

    /*
    class MultiOdeEvaluationSettings : EvaluationSettings {

        /// <summary>
        /// Gets or sets a function to be called with intermediate values as the
        /// system of ODEs is evaluated.
        /// </summary>
        public Action<EvaluationResult> UpdateHandler { get; set; }

        internal void Update (EvaluationResult result) {
            if (UpdateHandler != null) UpdateHandler(result);
        }

    }
    */
}
